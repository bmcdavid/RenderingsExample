@model RenderingsExample.Models.ViewModels.ContentPage

<section class="section">
    <div class="container">
        <div class="col-md-3">
            <nav class="nav-bar nav-bar--list">
                @Html.Partial("~/Views/Partials/Navigation/SubNavigation.cshtml")
            </nav>
        </div>
        <div class="col-md-9">
            <article>
                @Html.GetGridHtml(Model.Content, Model.BodyTextPropertyAlias, "bootstrap3-fluid")
            </article>
        </div>
    </div>
</section>