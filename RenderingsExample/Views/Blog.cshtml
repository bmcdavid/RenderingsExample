@model RenderingsExample.Models.ViewModels.Blog

@Html.Partial("~/Views/Partials/SectionHeader.cshtml")

<section class="section">
    <div class="container">
        <div class="blogposts">
            @foreach (var post in Model.BlogPosts)
            {
                @Html.Partial(post.GetPartialView(), post)
            }
        </div>
    </div>
</section>